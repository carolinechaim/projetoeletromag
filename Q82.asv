iterations = 100;
M = (1/freq_ress_sim) ; % rads/s
M_list = linspace(M,M/1000,iterations);

Rc = 1;
R1 = 1;
w = freq_ress_sim;

G_list = zeros(iterations,1);
Uf = 110;

for i=1:iterations
    M = M_list(i);
    i1 = (Uf)/(R1 - (1j*w*M)^2/Rc);
    i2 = (1j*w*M/Rc)*i1;
    G = (i2/i1)^2;
   
    G_list(i) = abs(G);
end

plot(M_list, G_list)

F = taylor(M_list)
